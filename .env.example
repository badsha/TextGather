# VoiceScript Collector Configuration
# Copy this file to .env and update the values

# Flask Environment
FLASK_ENV=production
FLASK_DEBUG=False

# Security (Generate a secure random key)
SECRET_KEY=your-secret-key-generate-a-random-one

# Database Configuration
DATABASE_URL=sqlite:///voicescript.db

# File Upload Settings
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=52428800

# Optional: For PostgreSQL instead of SQLite
# DATABASE_URL=postgresql://username:password@localhost/voicescript_db

# Optional: Custom port (default is 8000)
# PORT=8000

# Google OAuth Configuration (optional)
# Get these credentials from https://console.developers.google.com/
# Create a new project, enable Google+ API, and create OAuth 2.0 credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret