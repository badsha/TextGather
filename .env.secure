# SECURE PRODUCTION CONFIGURATION
# This file contains settings for PUBLIC internet deployment with HTTPS

# Database Configuration
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# Flask Configuration
SECRET_KEY=CHANGE_THIS_TO_RANDOM_STRING  # Generate with: openssl rand -hex 32
FLASK_ENV=production

# Security Settings (CRITICAL for public deployment)
USE_HTTPS=true  # MUST be true for public deployment
ENABLE_WEBVIEW_FALLBACK=false  # MUST be false for security

# Google OAuth (optional - update with your credentials)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Domain Configuration
DOMAIN=your-domain.com  # Your actual domain name
EMAIL=admin@your-domain.com  # For Let's Encrypt certificate notifications

# Application Configuration
UPLOAD_FOLDER=/app/uploads
PORT=8000
